<!DOCTYPE html>
<html>
<head>
  <title>Kuchnia</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">

  <meta name="turbo-cache-control" content="no-cache">

  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" defer></script>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="d-flex flex-column min-vh-100">

<nav class="navbar navbar-expand-lg navbar-dark shadow-sm"
     style="background: linear-gradient(135deg, #28a745, #85e085);">
  <div class="container">

    <%= link_to "Kuchnia", root_path, class: "navbar-brand fw-bold fs-3 text-white" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="mainNav">
      <ul class="navbar-nav ms-auto align-items-lg-center gap-3">



        <% if logged_in? %>
          <li class="nav-item text-white fw-semibold">
            Witaj <strong><%= current_user.username %></strong>
          </li>

          <% if current_user&.admin? %>
            <li class="nav-item">
              <%= link_to "Panel Admina", admin_users_path,
                          class: "nav-link text-white fw-semibold" %>
            </li>
          <% end %>

          <li class="nav-item">
            <%= link_to "Moje przepisy", my_recipes_path,
                        class: "nav-link text-white fw-semibold" %>
          </li>

          <li class="nav-item">
            <%= link_to "Wyloguj się", logout_path,
                        data: { turbo_method: :delete, turbo_action: "replace" },
                        class: "nav-link text-white fw-semibold" %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Zaloguj się", login_path,
                        data: { turbo_action: "replace" },
                        class: "nav-link text-white fw-semibold" %>
          </li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>

<main class="container my-5 flex-grow-1">
  <%= yield %>
</main>

<footer class="py-2" style="background: #000;">
  <div class="container text-center text-white">
    <p class="mb-0">&copy; <%= Time.now.year %> Kuchnia</p>
  </div>
</footer>

</body>
</html>
